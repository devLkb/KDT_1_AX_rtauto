# ML-Agents 학습 가상환경 — Python 3.10.11 또는 3.10.12 기준 (mlagents는 3.10.x 전용)
# ⚠️ mlagents는 protobuf 3.x 요구 → mediapipe(protobuf 4.x)와 같은 환경에 설치 금지.
#    반드시 별도 venv (venv-mlagents). protobuf/PyTorch는 mlagents가 맞는 버전을 자동 설치.
# ⚠️ 버전 고정 보류: Unity ML-Agents 패키지 버전과 Python mlagents 버전 짝을 반드시
#    맞춰야 함 (docs/ML_AGENTS_ROADMAP.md §3-1) — Unity 쪽 패키지 설치 시점에 함께 고정할 것.
mlagents
